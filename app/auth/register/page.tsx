import React from "react";
import * as yup from "yup";

export const registerSchema = yup.object().shape({
  
});

const Register = () => {
  return <div>ini adalah halaman register</div>;
};

export default Register;
